# Core computer vision
opencv-python==4.10.0.84       # webcam capture, frame drawing, imshow

# Hand landmark detection (CPU-friendly, no GPU needed)
mediapipe==0.10.32             # 21-point hand skeleton per frame (Apple Silicon compatible)

# Numerical arrays (keep <2.0 — mediapipe 0.10.x requires it)
numpy==1.26.4                  # frame slicing, landmark arrays

# Initial ML baseline (prototype requirement)
scikit-learn                   # baseline training/evaluation pipeline
matplotlib                     # plotting support for analysis/notebooks
